// Test stub: the real `server-only` package throws when imported outside a
// Server Component. Under Vitest we replace it with a no-op so the pure logic
// in server-only-tagged modules can be unit-tested.
export {};
